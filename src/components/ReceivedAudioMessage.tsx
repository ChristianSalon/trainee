import React from "react";
import { View, Text } from "react-native";

const ReceivedAudioMessage = ({ message }) => {
  return (
    <View>
      <Text>Received Audio Message</Text>
    </View>
  );
};

export default ReceivedAudioMessage;
